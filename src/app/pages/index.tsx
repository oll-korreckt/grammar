import React from "react";
import ReactDOM from "react-dom";

const App = () => {
    return <>React App</>;
};

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById("root")
);
